type Query {
  Poll(poll: Int!): Poll!
  Genres: [Genre!]!

  SearchPoll(query: String!): [Poll]!
  SearchPlatforms(query: String!): [Platform]!
  SearchGames(poll: Int!, query: String!): [Game]!

  Login(nick: String!, password: String!): User
  MyPolls: [Poll]!
}
